{
  "name": "australian-cultural-diary-juliet-sostena",
  "version": "1.0.0",
  "private": true,
  "description": "Single-page React + Vite app — Australian Cultural Diary — Juliet Sostena",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "format": "prettier --write \"src/**/*.{js,jsx,css,md}\"",
  "deploy": "npm run build && git add dist -f && git commit -m 'Build for GitHub Pages' && git subtree push --prefix dist origin gh-pages"
},
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "framer-motion": "^12.23.24",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.23",
    "tailwindcss": "^3.4.7",
    "vite": "^5.0.0"
  }
}
